import BovedaApp from "@/components/boveda/boveda-app";

export const metadata = {
  title: "BÓVEDA — La memoria de IA que es tuya",
  description: "Bóveda cifrada E2E de memorias de IA: extrae, cifra, respalda y continúa en cualquier agente.",
};

export default function BovedaPage() {
  return <BovedaApp />;
}
